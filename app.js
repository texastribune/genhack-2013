// Generated by CoffeeScript 1.4.0
(function() {
  var Game, Round, main, rounds;

  rounds = [
    {
      tiles: [
        {
          src: '',
          correct: true
        }
      ],
      success: 'You won!'
    }
  ];

  Game = (function() {

    function Game() {
      this.rounds = [];
    }

    Game.prototype.addRound = function(round) {
      return this.rounds.push(round);
    };

    return Game;

  })();

  Round = (function() {

    function Round(data) {
      this.tiles = data.tiles;
      this.success = data.success;
    }

    Round.prototype.display = function() {
      return console.log(this.tiles, this.success);
    };

    return Round;

  })();

  main = function() {
    var g, r, round, _i, _len, _results;
    g = new Game();
    _results = [];
    for (_i = 0, _len = rounds.length; _i < _len; _i++) {
      round = rounds[_i];
      r = new Round(round);
      g.addRound(r);
      _results.push(r.display());
    }
    return _results;
  };

  $(main);

}).call(this);
